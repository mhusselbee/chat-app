export enum ApiRoutes {
  // Auth routes
  SIGN_UP = '/signup',
  SIGN_IN = '/signin',
  
  // Conversation routes
  GET_CONVERSATIONS = '/',
  CREATE_CONVERSATION = '/',
  
  // User routes
  VALIDATE_USERS = '/validate',
  
  // Update user
  UPDATE_USER = '/update'
}